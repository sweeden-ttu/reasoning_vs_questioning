# Self-training / Reasoning vs Questioning dependencies
# Install into conda env `kagg`:
#   KAGG_PY="$(conda info --base)/envs/kagg/bin/python"
#   uv pip install --find-links ../wheels/pyahocorasick -r requirements.txt --python "$KAGG_PY"
#
# Vendored dual-arch wheels live at repo-root wheels/pyahocorasick/
# (Linux x86_64 + aarch64; macOS universal2).

numpy>=1.26
torch
gymnasium
kaggle-environments>=1.32
matplotlib
scikit-learn>=1.5
tqdm

# Aho-Corasick automata — import as `ahocorasick`
--find-links=../wheels/pyahocorasick
pyahocorasick==2.3.1
